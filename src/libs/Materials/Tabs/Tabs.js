import Div from "../../ui/Tags/Div/Div";

class Tabs extends Div {

    constructor(tabs = []) {
        super();

        if (tabs.length > 0) {
            
        } 

    }

}

export default Tabs;