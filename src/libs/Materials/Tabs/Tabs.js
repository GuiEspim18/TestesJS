import Div from "../../ui/Tags/Div/Div.js";

class Tabs extends Div {

    constructor(tabs = [], pages = []) {
        super();

        if (tabs.length > 0) {

        } 

        if (pages.length > 0) {
            
        }

    }

}

export default Tabs;